steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - platform/infra/docker/Dockerfile.platform
      - -t
      - ${_IMAGE_URI}
      - .
images:
  - ${_IMAGE_URI}
