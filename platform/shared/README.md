# Shared

This folder will hold artifacts shared between backend, web, and mobile.

Planned contents:

- `openapi/` for exported schemas
- `types/` for shared or generated client types
- `docs/` for API notes and contract documentation

Status:

- folder scaffold only
