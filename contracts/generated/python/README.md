# Python Contract Models

The first contract migration slice keeps the Python models in
`platform/backend/app/contracts/device_protocol.py` as hand-written Pydantic
models that mirror the JSON Schema files.

TODO: add JSON Schema to Pydantic generation once the contract folder is stable.
